# 🚀 快速开始指南

## 方式一：自动部署（推荐）

### Linux/Mac 用户：
```bash
chmod +x deploy.sh
./deploy.sh
```

### Windows 用户：
1. 确保已安装 Node.js 16+
2. 打开命令提示符或 PowerShell
3. 执行以下命令：

```powershell
# 安装后端依赖
cd backend
npm install
cd ..

# 安装前端依赖
cd frontend
npm install
cd ..

# 初始化数据库
cd backend
node -e "const db = require('./config/database'); db.initDatabase();"
cd ..
```

---

## 方式二：手动部署

### 1. 安装后端依赖
```bash
cd backend
npm install
```

### 2. 安装前端依赖
```bash
cd frontend
npm install
```

### 3. 初始化数据库
```bash
cd backend
node -e "const db = require('./config/database'); db.initDatabase();"
```

---

## 启动服务

### 启动后端（端口 3000）
```bash
cd backend
npm start
```

### 启动前端（端口 5173）- 新开一个终端
```bash
cd frontend
npm run dev
```

---

## 访问系统

- **前台抽奖页面**：http://localhost:5173
- **后台管理登录**：http://localhost:5173/admin/login

**默认管理员账号**：
- 用户名：`admin`
- 密码：`admin123`

⚠️ **重要**：生产环境请立即修改默认密码！

---

## 常见问题

### Q: 端口被占用怎么办？
修改端口配置：
- 后端：修改 `backend/app.js` 中的端口号
- 前端：修改 `frontend/vite.config.js` 中的端口号

### Q: 前端无法连接后端？
检查 `frontend/src/services/adminApi.js` 和 `lotteryApi.js` 中的 API_BASE_URL 是否正确。

### Q: 数据库初始化失败？
删除 `backend/lottery.db` 文件后重新运行初始化命令。

---

## 更多文档

- [README.md](./README.md) - 完整功能介绍
- [DEPLOYMENT.md](./DEPLOYMENT.md) - 详细部署指南
